# Apollo/Express Server

required `.env` keys:
```
googleClientId
googleClientSecret
```
## First time?
### In this order:
- add `.env` file to root directory
- docker-compose up
- npm run prisma:schema
- npm run prisma:up
- npm run start:dev
